name: "🐞 bug"
description: "버그 수정 (제목은 'bug: SET 명령 시 TTL이 적용되지 않음' 형태로 입력해 주세요)"
title: "bug: "
labels: ["bug"]
body:
  - type: textarea
    attributes:
      label: 🔍 기능
      description: |
        어느 기능에서 버그가 발생했는지 작성해 주세요.
        예: GET 명령 처리, CacheStore의 데이터 만료(TTLManager), 다중 클라이언트 접속
    validations:
      required: true
  - type: textarea
    attributes:
      label: 📄 설명
      description: |
        발생한 버그에 대한 설명을 작성해 주세요.
        예: SET name value EX 5 명령 실행 후, 5초가 지나도 데이터가 삭제되지 않고 GET 요청 시 조회됨 (TTLManager 문제)
        예: 다중 클라이언트 접속 시 CacheStore에 데이터 손실이 발생함 (동시성 문제)
    validations:
      required: true
  - type: textarea
    attributes:
      label: ✅ 작업할 내용
      description: |
        수정할 일을 체크박스 형태로 작성해주세요.
        예:
        - [ ] TTLManager의 만료 로직 및 스케줄링 간격 확인
        - [ ] CacheStore의 HashMap 접근에 대한 동시성 제어 (synchronized/Lock) 추가
    validations:
      required: true
  - type: textarea
    attributes:
      label: 💡 참고 자료
      description: |
        참고 자료가 있다면 작성해 주세요.
        예:
        - 관련 에러 로그 (Stack Trace)
        - 재현 시나리오
        - 관련 코드 스니펫 (예: CacheStore.java)
        참고 자료가 없는 경우 `_No response_`로 작성해 주세요.